[project]
name = "arcron-vesting"
version = "0.1.0"
description = "TestNet-only Arcron vesting. Schedule accounts; beneficiary pulls."
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
compile = ["puyapy"]
dev = ["pytest>=8.0.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
